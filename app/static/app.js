const app=document.querySelector("#app"),toast=document.querySelector("#toast");
const state={analysis:null,overview:null,detections:[],analyses:[]};
const esc=v=>{const n=document.createElement("span");n.textContent=v??"";return n.innerHTML};
const api=async(url,options={})=>{const r=await fetch(url,options),d=await r.json();if(!r.ok)throw Error(d.detail||"Request failed");return d};
const route=()=>location.hash.replace("#/","").split("?")[0]||"home";
const notify=(m,t="ok")=>{toast.textContent=m;toast.className="show "+t;setTimeout(()=>toast.className="",3500)};
const page=(title,kicker,body)=>`<section class="app-page"><div class="page-head"><div><p class="eyebrow">${kicker}</p><h1>${title}</h1></div>${datasetPicker()}</div>${body}</section>`;
const empty=t=>`<div class="empty"><span>◎</span><h3>No evidence analysis yet</h3><p>${t}</p><a class="button" href="#/upload">Analyze evidence</a></div>`;
function datasetPicker(){return state.analyses.length?`<label class="dataset-picker">Dataset<select id="datasetSelect">${state.analyses.map(a=>`<option value="${a.id}" ${state.analysis?.id===a.id?"selected":""}>${esc(a.filename)} · ${a.event_count} rows</option>`).join("")}</select></label>`:""}
async function load(id){state.analyses=await api("/api/analyses");const chosen=id||state.analysis?.id||state.analyses[0]?.id;if(chosen){state.analysis=await api("/api/analyses/"+chosen);state.overview=await api("/api/overview");state.detections=(await api("/api/detections?analysis_id="+chosen)).findings}else{state.analysis=null;state.detections=[];state.overview=await api("/api/overview")}}
function landing(){return `<section class="hero"><div class="hero-copy"><p class="eyebrow">ADVANCED NUCLEAR SECURITY PLATFORM</p><h1>Unified assurance for nuclear digital infrastructure</h1><p class="lede">A defensive read only platform connecting passive SCADA monitoring, safety system integrity, nuclear material safeguards, explainable threat detection and regulatory evidence for civil and defense applications.</p><div class="hero-actions"><a class="button" href="#/upload">Analyze evidence</a><a class="text-link" href="#/dashboard">Open assurance overview ↗</a></div><div class="principle"><span>Safety authority</span><strong>Technology prioritizes the evidence. Qualified people authorize every response.</strong></div></div><div class="plant-wrap"><div class="orbit"></div><img src="/assets/floating-plant.png" alt="Original fictional floating nuclear facility"><div class="float-label one"><b>ONE WAY</b><span>Evidence transfer</span></div><div class="float-label two"><b>PASSIVE</b><span>Read only analysis</span></div></div></section>
<section class="feature-band"><a href="#/scada"><b>01</b><span>SCADA protection<small>Review passive OT evidence, baselines and network anomalies.</small></span></a><a href="#/integrity"><b>02</b><span>Safety-system integrity<small>Compare approved logic, firmware and configuration states.</small></span></a><a href="#/safeguards"><b>03</b><span>Nuclear material security<small>Correlate PACS, MC&amp;A, radiation and cyber evidence.</small></span></a><a href="#/ai"><b>04</b><span>AI threat detection<small>Review explainable anomalies, intelligence context and authorization.</small></span></a></section>
<section class="landing-section"><p class="eyebrow">COMPLETE ASSURANCE WORKFLOW</p><h2>From isolated evidence to a defensible decision</h2><p class="section-intro">NuclearShield receives exported evidence across a conceptual one-way boundary, preserves its digest, applies five transparent detection methods, and presents findings to qualified reviewers. Each result can be traced to its source record and included in an assurance report.</p><div class="capability-grid">${[["Passive collection","Zeek/Suricata-style network metadata and exported operational evidence."],["Hybrid detection","Baseline rules, integrity validation, authorization checks and robust outlier scoring."],["Cross-domain correlation","Physical access, cyber activity, radiation and MC&A evidence connected by actor or asset."],["Governed response","Confidence, contributing factors, audit history and mandatory human/safety authorization."],["Compliance evidence","IEC 62645, NRC RG 5.71 and IAEA themes mapped without claiming certification."],["Operational monitoring","Prometheus telemetry and a provisioned twenty-panel Grafana dashboard."]].map(x=>`<article><span>◆</span><h3>${x[0]}</h3><p>${x[1]}</p></article>`).join("")}</div></section>
<section class="workflow-band"><div><p class="eyebrow">HOW THE DEMONSTRATION WORKS</p><h2>One file activates the complete review chain</h2><p>Upload the supplied full-platform dataset, select it from any page, inspect its evidence and findings, then open the generated report and monitoring tools.</p></div><ol><li><b>01</b><span>Ingest and hash<small>Validate CSV or JSON, normalize fields and calculate SHA-256 provenance.</small></span></li><li><b>02</b><span>Detect and correlate<small>Apply rules, statistical analysis and physical-cyber relationships.</small></span></li><li><b>03</b><span>Review and report<small>Explain each finding, preserve audit history and require human authority.</small></span></li></ol></section>`}
function upload(){return page("Analyze evidence","NORMALIZE · VALIDATE · DETECT · CORRELATE",`<div class="upload-layout"><form id="uploadForm" class="upload-card"><label id="dropzone" class="drop" for="evidence"><span class="drop-icon">＋</span><strong>Drop CSV or JSON evidence</strong><small>Flexible column names · maximum 10 MB · up to 50,000 rows</small><input id="evidence" type="file" accept=".csv,.json" required></label><div id="fileName" class="file-name">No file selected</div><button class="button">Run complete analysis</button><button id="demo" class="button ghost" type="button">Analyze 24-row synthetic demonstration</button><p id="uploadStatus">Every accepted row is normalized, hashed, scored and persisted.</p></form><aside class="schema"><h2>Analysis engines</h2>${[["Schema adapter","Infers common ID, time, source, value and event-class fields."],["Baseline engine","Compares numeric and categorical observations."],["Integrity engine","Checks declared hashes, signatures and approved states."],["AI-assisted model","Median/MAD robust anomaly scoring for groups with 5+ numeric rows."],["Correlation engine","Links unauthorized access with asset or actor activity."],["Governance gate","No automatic containment; human authorization is mandatory."]].map(x=>`<div class="schema-row"><b>${x[0]}</b><span>${x[1]}</span></div>`).join("")}</aside></div>`)}
const bar=(l,v,total,c="teal")=>`<div class="bar-row"><div><span>${esc(l)}</span><b>${v}</b></div><i><em class="${c}" style="width:${total?Math.max(2,Math.round(v/total*100)):0}%"></em></i></div>`;
function queue(list=state.detections){return `<div class="panel table-panel"><div class="panel-head"><h2>Detection queue</h2><span>${list.length} findings</span></div><div class="table-wrap"><table><thead><tr><th>Severity</th><th>Event</th><th>Detection</th><th>Engine</th><th>Confidence</th><th></th></tr></thead><tbody>${list.map(f=>`<tr><td><span class="tag ${f.severity}">${f.severity}</span></td><td><b>${esc(f.event_id)}</b><small>${esc(f.event.timestamp)}</small></td><td>${esc(f.detected)}</td><td><code>${esc(f.engine)}</code></td><td><strong>${f.confidence}%</strong></td><td><button class="row-link" data-event="${esc(f.event_id)}">Inspect</button></td></tr>`).join("")||'<tr><td colspan="6">No findings match this view.</td></tr>'}</tbody></table></div></div>`}
function dashboard(){if(!state.analysis)return page("Command center","LIVE FROM THE SELECTED DATASET",empty("Upload one or more evidence files to populate the platform."));const s=state.analysis.summary||{},sev=s.severities||{};return page("Command center","LIVE FROM THE SELECTED DATASET",`<div class="analysis-banner"><div><span>ACTIVE DATASET</span><strong>${esc(state.analysis.filename)}</strong><small>SHA-256 ${state.analysis.digest}</small></div><a href="/api/report?analysis_id=${state.analysis.id}" target="_blank">Open full report ↗</a></div><div class="metric-grid"><article><span>Rows processed</span><strong>${s.accepted_records||0}</strong><small>${s.processing_coverage||0}% processing coverage</small></article><article><span>Findings</span><strong>${state.analysis.finding_count}</strong><small>${s.finding_rate||0}% of accepted records</small></article><article><span>Critical / high</span><strong class="critical-text">${(sev.critical||0)+(sev.high||0)}</strong><small>Qualified review required</small></article><article><span>Rejected rows</span><strong>${s.rejected_records||0}</strong><small>Visible, never silently dropped</small></article></div><div class="dashboard-grid"><article class="panel"><div class="panel-head"><h2>Finding severity</h2></div>${["critical","high","medium","low"].map((x,i)=>bar(x,sev[x]||0,state.analysis.finding_count,["red","amber","teal","teal"][i])).join("")}</article><article class="panel"><div class="panel-head"><h2>Evidence processed by domain</h2></div>${Object.entries(s.evidence_types||{}).map(([k,v])=>bar(k,v,s.accepted_records)).join("")}</article></div>${queue(state.detections.slice(0,6))}`)}
function detections(kind=null){if(!state.analysis)return page("Detections","EXPLAINABLE REVIEW QUEUE",empty("Analyze evidence to generate findings."));const list=kind?state.detections.filter(f=>f.event.event_type===kind):state.detections;return page(kind==="integrity"?"Safety-system integrity":"Detections","FILTERED · EVIDENCE-LINKED · HUMAN-GATED",`<div class="filterbar"><input id="search" placeholder="Search event, source, reason"><select id="typeFilter"><option value="">All evidence types</option>${["network","integrity","access","material","radiation","change"].map(x=>`<option ${x===kind?"selected":""}>${x}</option>`).join("")}</select><select id="severityFilter"><option value="">All severities</option><option>critical</option><option>high</option><option>medium</option><option>low</option></select></div><div id="queue">${queue(list)}</div>`)}
function evidence(){if(!state.analysis)return page("Evidence explorer","ALL ACCEPTED RECORDS",empty("Upload evidence to inspect normalized records."));const rows=state.analysis.events;return page("Evidence explorer","NORMALIZED RECORDS FROM THE SELECTED FILE",`<div class="metric-grid"><article><span>Accepted</span><strong>${rows.length}</strong></article><article><span>Rejected</span><strong>${state.analysis.rejected?.length||0}</strong></article><article><span>Coverage</span><strong>${state.analysis.summary.processing_coverage}%</strong></article><article><span>File digest</span><strong>${state.analysis.digest.slice(0,8)}</strong></article></div><div class="table-wrap panel"><table><thead><tr><th>Event</th><th>Time</th><th>Domain</th><th>Source / asset</th><th>Observed</th><th>Baseline</th></tr></thead><tbody>${rows.map(e=>`<tr><td><b>${esc(e.event_id)}</b><small>row ${e.raw_row}</small></td><td>${esc(e.timestamp)}</td><td><span class="tag">${esc(e.event_type)}</span></td><td>${esc(e.source)}<small>${esc(e.asset)}</small></td><td>${esc(e.value)}</td><td>${esc(e.baseline)}</td></tr>`).join("")}</tbody></table></div>`)}
function scada(){if(!state.analysis)return page("Nuclear SCADA protection","PASSIVE OT MONITORING · DEFENSE IN DEPTH",empty("Analyze evidence to activate the SCADA protection view."));const rows=state.analysis.events.filter(e=>e.event_type==="network"),findings=state.detections.filter(f=>f.event.event_type==="network"),sources=new Set(rows.map(e=>e.source)),assets=new Set(rows.map(e=>e.asset));return page("Nuclear SCADA protection","ZEEK / SURICATA EVIDENCE · ONE-WAY ISOLATION",`<div class="requirement-banner"><div><span>IMPLEMENTED REQUIREMENT</span><h2>Passive SCADA and I&amp;C evidence analysis</h2><p>Exported Zeek/Suricata-style metadata crosses a conceptual unidirectional boundary into NuclearShield. The platform compares activity with declared baselines, detects robust peer-group outliers and never sends a command back to control or safety systems.</p></div><strong>READ-ONLY</strong></div><div class="metric-grid"><article><span>SCADA network records</span><strong>${rows.length}</strong><small>Selected dataset</small></article><article><span>Passive sources</span><strong>${sources.size}</strong><small>${esc([...sources].join(", ")||"none")}</small></article><article><span>Observed OT assets</span><strong>${assets.size}</strong><small>Evidence-derived inventory</small></article><article><span>SCADA findings</span><strong>${findings.length}</strong><small>Baseline and anomaly review</small></article></div><div class="assurance-flow"><article><b>01</b><h3>Control and safety zones</h3><p>SCADA, reactor-protection and safety instrumentation remain independent from this demonstration platform.</p></article><article><b>02</b><h3>One-way evidence export</h3><p>Only passive metadata and integrity evidence move outward through the conceptual data-diode boundary.</p></article><article><b>03</b><h3>Explainable detection</h3><p>Baseline deviation and median/MAD scoring identify unusual activity with visible reasons and confidence.</p></article><article><b>04</b><h3>Authorized disposition</h3><p>Qualified operators validate context and use approved safety-preserving procedures outside NuclearShield.</p></article></div>${queue(findings)}`)}
function ai(){if(!state.analysis)return page("AI-driven nuclear threat detection","ML ANOMALIES · THREAT CONTEXT · HUMAN AUTHORIZATION",empty("Analyze evidence to activate all inference engines."));const s=state.analysis.summary,aiFindings=state.detections.filter(f=>f.engine.includes("robust")||f.engine.includes("correlation")),networkFindings=state.detections.filter(f=>f.event.event_type==="network").length;return page("AI-driven nuclear threat detection","EXPLAINABLE AND SAFETY-GOVERNED",`<div class="notice"><b>Governance boundary:</b> This is an explainable hybrid demonstrator, not a reactor-trained model. It cannot execute containment or change a plant system.</div><div class="metric-grid"><article><span>AI/correlation findings</span><strong>${aiFindings.length}</strong><small>Selected dataset</small></article><article><span>Network indicators</span><strong>${networkFindings}</strong><small>Evidence requiring context</small></article><article><span>Detection engines</span><strong>${Object.values(s.engines).filter(v=>v>0).length}</strong><small>Active for this file</small></article><article><span>Autonomous actions</span><strong>0</strong><small>Human and safety authority required</small></article></div><div class="dashboard-grid"><article class="panel"><h2>Engine activity</h2>${Object.entries(s.engines).map(([k,v])=>bar(k,v,state.analysis.finding_count)).join("")}</article><article class="panel"><h2>Inference workflow</h2><ol class="steps"><li>Normalize every accepted row</li><li>Apply deterministic safety and authorization rules</li><li>Build robust peer-group statistical profiles</li><li>Correlate actors and assets across evidence domains</li><li>Add local nuclear-sector threat context</li><li>Require human and safety authorization</li></ol></article></div><div class="assurance-flow"><article><b>THREAT CONTEXT</b><h3>Local intelligence catalogue</h3><p>Demonstrates offline matching for unusual OT communications, unauthorized engineering activity and compromised credentials without requiring cloud access.</p></article><article><b>ML ANALYSIS</b><h3>Robust anomaly scoring</h3><p>Median and median-absolute-deviation profiles identify peer-group outliers when sufficient numeric evidence exists.</p></article><article><b>EXPLANATION</b><h3>Traceable contributors</h3><p>Every finding exposes its engine, confidence, reasons, contributing evidence and related events.</p></article><article><b>SAFE RESPONSE</b><h3>Safety-preserving containment</h3><p>The platform prepares evidence for approved external procedures; qualified people authorize isolation, recovery and return to service.</p></article></div>${queue(aiFindings)}`)}
function devsecops(){if(!state.analysis)return page("Nuclear software DevSecOps","CHANGE CONTROL · STATIC ANALYSIS · RECOVERY",empty("Analyze evidence to populate the change and configuration-control view."));const rows=state.analysis.events.filter(e=>e.event_type==="change"||e.event_type==="integrity"),findings=state.detections.filter(f=>["change","integrity"].includes(f.event.event_type)),approved=rows.filter(e=>e.authorized&&["valid","ok","match","true"].includes(e.integrity)).length;return page("Nuclear software DevSecOps","REGULATED SOFTWARE ASSURANCE",`<div class="requirement-banner"><div><span>IMPLEMENTED REQUIREMENT</span><h2>Controlled software and configuration evidence</h2><p>Change, firmware and configuration records are checked against approval and integrity evidence. Repository automation independently runs unit tests, linting and static security analysis before a change can be accepted.</p></div><strong>HUMAN GATE</strong></div><div class="metric-grid"><article><span>Change / integrity records</span><strong>${rows.length}</strong><small>Selected dataset</small></article><article><span>Approved and valid</span><strong>${approved}</strong><small>Authorization plus integrity</small></article><article><span>Review findings</span><strong>${findings.length}</strong><small>Mismatch or control failure</small></article><article><span>Automated CI gates</span><strong>3</strong><small>Tests · lint · security scan</small></article></div><div class="assurance-flow"><article><b>01</b><h3>Propose and identify</h3><p>Every change needs an attributable identity, work authorization and controlled baseline.</p></article><article><b>02</b><h3>Verify automatically</h3><p>Tests, code-quality checks and static security analysis run through the GitHub workflow.</p></article><article><b>03</b><h3>Approve independently</h3><p>Safety-significant changes require qualified review and segregation of duties.</p></article><article><b>04</b><h3>Recover predictably</h3><p>Signed golden baselines and approved rollback procedures support validated recovery.</p></article></div>${queue(findings)}`)}
function safeguards(){if(!state.analysis)return page("Safeguards & insider risk","PHYSICAL-CYBER / MC&A CORRELATION",empty("Analyze evidence to populate safeguards views."));const rows=state.analysis.events.filter(e=>["access","material","radiation"].includes(e.event_type));return page("Safeguards & insider risk","ATTRIBUTABLE CROSS-DOMAIN EVIDENCE",`<div class="metric-grid">${["access","material","radiation"].map(k=>`<article><span>${k} records</span><strong>${rows.filter(e=>e.event_type===k).length}</strong></article>`).join("")}<article><span>Correlated findings</span><strong>${state.detections.filter(f=>f.related_events?.length).length}</strong></article></div>${queue(state.detections.filter(f=>["access","material","radiation"].includes(f.event.event_type)||f.related_events?.length))}`)}
async function compliance(){const c=await api("/api/controls");return page("Compliance evidence","IEC 62645 · NRC RG 5.71 · IAEA",`<p class="page-intro">A defensible control-to-evidence crosswalk for learning and review; it does not assert certification.</p><div class="table-wrap panel"><table><thead><tr><th>Domain</th><th>IEC 62645</th><th>NRC RG 5.71</th><th>IAEA</th><th>Evidence</th></tr></thead><tbody>${c.map(x=>`<tr><td><b>${esc(x.domain)}</b></td><td>${esc(x.iec)}</td><td>${esc(x.nrc)}</td><td>${esc(x.iaea)}</td><td>${esc(x.evidence)}</td></tr>`).join("")}</tbody></table></div>`)}
function reports(){return page("Assurance reports","DETAILED FINDINGS · MEASURES · REGULATORY EVIDENCE",state.analyses.map(a=>`<article class="report-card"><div><span>${new Date(a.created_at).toLocaleString()}</span><h2>${esc(a.filename)}</h2><p>${a.event_count} records · ${a.finding_count} findings</p><code>${a.digest}</code></div><div class="report-actions"><a class="button small" target="_blank" href="/api/report?analysis_id=${a.id}">Open report ↗</a><a class="button small ghost" target="_blank" href="/api/report?analysis_id=${a.id}&print_view=true">Print</a><a class="button small ghost" href="/api/report/download?analysis_id=${a.id}">Download HTML</a></div></article>`).join("")||empty("Complete an analysis to generate a detailed report."))}
const q=expression=>"http://localhost:9090/query?g0.expr="+encodeURIComponent(expression)+"&g0.show_tree=0&g0.tab=table";
async function monitoring(){const [s,m]=await Promise.all([api("/api/platform-status"),api("/api/monitoring-summary")]),l=m.latest;const links=[["Prometheus targets","Scrape status for NuclearShield and Prometheus.","http://localhost:9090/targets"],["Query: application UP","Returns 1 when NuclearShield is reachable.",q("up{job=\"nuclearshield\"}")],["Query: findings","Latest findings grouped by severity.",q("nuclearshield_findings")],["Query: detection engines","Shows which engines contributed to findings.",q("nuclearshield_findings_by_engine")],["Query: processed events","Cumulative processed records by evidence domain.",q("nuclearshield_events_processed_total")],["Query: ingestions","Accepted and rejected evidence-file totals.",q("nuclearshield_ingestions_total")],["Raw application metrics","Direct Prometheus text exposition from NuclearShield.","http://localhost:8000/metrics"],["Grafana dashboard","Twenty-panel visual monitoring dashboard.","http://localhost:3000/d/nuclearshield/nuclearshield-evidence-assurance?orgId=1&refresh=5s"]];return page("Platform monitoring","APP TELEMETRY · PROMETHEUS · GRAFANA",`<p class="page-intro">Use this page as the monitoring launchpad. Status cards show live service health, the charts summarize stored application evidence, and every tool below opens at the exact page or prepared query needed for the demonstration.</p><div class="notice"><b>Live status:</b> Prometheus scrapes NuclearShield every five seconds inside Docker. Grafana reads those Prometheus metrics through its provisioned data source.</div><div class="status-grid">${Object.entries(s).map(([k,v])=>`<article><i class="${["healthy","ready","none","true"].includes(String(v))?"":"down"}"></i><span>${esc(k.replaceAll("_"," "))}</span><strong>${esc(String(v))}</strong></article>`).join("")}</div><div class="metric-grid">${Object.entries(m.totals).map(([k,v])=>`<article><span>Total ${esc(k)}</span><strong>${v}</strong><small>Persistent application data</small></article>`).join("")}</div><div class="dashboard-grid"><article class="panel"><div class="panel-head"><h2>Latest analysis severity</h2><span>${esc(l.filename||"No dataset")}</span></div><p class="panel-copy">Finding priority for the currently selected evidence analysis.</p>${Object.entries(l.severity).map(([k,v])=>bar(k,v,l.findings,k==="critical"?"red":k==="high"?"amber":"teal")).join("")}</article><article class="panel"><div class="panel-head"><h2>Detection engine activity</h2><span>${l.coverage}% processed</span></div><p class="panel-copy">Number of latest findings supported by each transparent detection method.</p>${Object.entries(l.engines).map(([k,v])=>bar(k,v,l.findings)).join("")||"<p>No analysis yet.</p>"}</article></div><div class="monitor-launch"><div><p class="eyebrow">ONE-CLICK EXAM LINKS</p><h2>Open a prepared monitoring view</h2><p>Each link opens in a new tab. Prometheus query links arrive with the expression already entered and executed by the query URL.</p></div><div class="launch-grid">${links.map(([name,desc,url])=>`<a class="launch-card" href="${url}" target="_blank" rel="noopener"><span>OPEN ↗</span><h3>${name}</h3><p>${desc}</p></a>`).join("")}</div></div><div class="panel query-list"><h2>Grafana access</h2><p class="panel-copy">The provisioned dashboard refreshes every five seconds and shows availability, ingestion, severity, engines, domains, latency, failures and recovery.</p><code>Username: admin</code><code>Password: nuclearshield-demo</code></div>`)}
async function audit(){const [rows,s]=await Promise.all([api("/api/audit?limit=500"),api("/api/audit-summary")]);return page("Audit trail","ATTRIBUTABLE · HASH-LINKED · SEARCHABLE",`<div class="metric-grid"><article><span>Audit entries</span><strong>${s.entries}</strong><small>Persistent records</small></article><article><span>Actors</span><strong>${s.actors}</strong><small>Attributable identities</small></article><article><span>Action classes</span><strong>${Object.keys(s.actions).length}</strong><small>${esc(Object.keys(s.actions).join(", ")||"none")}</small></article><article><span>Latest record</span><strong>${s.latest_timestamp?"RECENT":"NONE"}</strong><small>${esc(s.latest_timestamp||"No entries")}</small></article></div><div class="filterbar"><input id="auditSearch" placeholder="Search filename, digest, actor or action"><span id="auditCount">${rows.length} entries</span></div><div id="auditTable" class="table-wrap panel">${auditTable(rows)}</div><div class="notice"><b>Audit boundary:</b> ${esc(s.chain_note)}. This educational SQLite log is append-only through the application API, but it is not an external WORM store.</div>`)}
function auditTable(rows){return `<table><thead><tr><th>UTC time</th><th>Action / actor</th><th>Evidence SHA-256</th><th>Analysis details</th></tr></thead><tbody>${rows.map(a=>`<tr><td>${esc(new Date(a.timestamp).toISOString().replace('T', ' '))}</td><td><b>${esc(a.action)}</b><small>${esc(a.actor)}</small></td><td><code>${esc(a.evidence_digest)}</code></td><td>${Object.entries(a.details).map(([k,v])=>`<span class="audit-chip"><b>${esc(k)}</b> ${esc(String(v))}</span>`).join("")}</td></tr>`).join("")||'<tr><td colspan="4">No audit entries match this search.</td></tr>'}</tbody></table>`}
function showDetail(id){const f=state.detections.find(x=>x.event_id===id);if(!f)return;const d=document.createElement("dialog");d.innerHTML=`<button class="close">×</button><span class="tag ${f.severity}">${f.severity} · score ${f.score}</span><h2>${esc(f.detected)}</h2><dl><div><dt>Event / source</dt><dd>${esc(f.event_id)} · ${esc(f.event.source)}</dd></div><div><dt>Engine / confidence</dt><dd>${esc(f.engine)} · ${f.confidence}%</dd></div><div><dt>Why it triggered</dt><dd>${esc(f.reasons.join("; "))}</dd></div><div><dt>Contributors</dt><dd>${esc(f.contributors.join("; "))}</dd></div><div><dt>Related events</dt><dd>${esc(f.related_events.join(", ")||"none")}</dd></div><div><dt>Recommended measures</dt><dd>${esc(f.measures)}</dd></div></dl><div class="authorization">Human and safety authorization required. NuclearShield is read-only.</div>`;document.body.append(d);d.showModal();d.querySelector(".close").onclick=()=>{d.close();d.remove()}}
function bind(){const pick=document.querySelector("#datasetSelect");if(pick)pick.onchange=async()=>{await load(pick.value);render(false)};document.querySelectorAll("[data-event]").forEach(b=>b.onclick=()=>showDetail(b.dataset.event));const form=document.querySelector("#uploadForm"),input=document.querySelector("#evidence"),demo=document.querySelector("#demo"),drop=document.querySelector("#dropzone");if(input)input.onchange=()=>document.querySelector("#fileName").textContent=input.files[0]?.name||"No file selected";if(drop){["dragenter","dragover"].forEach(n=>drop.addEventListener(n,e=>{e.preventDefault();drop.classList.add("drag")}));["dragleave","drop"].forEach(n=>drop.addEventListener(n,e=>{e.preventDefault();drop.classList.remove("drag")}));drop.addEventListener("drop",e=>{if(e.dataTransfer.files[0]){const dt=new DataTransfer();dt.items.add(e.dataTransfer.files[0]);input.files=dt.files;input.onchange()}})}if(form)form.onsubmit=e=>{e.preventDefault();if(input.files[0])submit(input.files[0])};if(demo)demo.onclick=async()=>{const r=await fetch("/sample-data/synthetic-evidence.csv");submit(new File([await r.blob()],"synthetic-evidence.csv",{type:"text/csv"}))};["typeFilter","severityFilter","search"].forEach(id=>{const n=document.querySelector("#"+id);if(n)n.oninput=filter})}
async function submit(file){const status=document.querySelector("#uploadStatus"),body=new FormData();body.append("file",file);status.textContent="Normalizing and running five analysis engines…";try{const d=await api("/api/ingest",{method:"POST",body});status.textContent=`Complete: ${d.events} accepted, ${d.rejected.length} rejected, ${d.findings.length} findings, ${d.summary.processing_coverage}% coverage.`;notify("Complete evidence analysis finished");state.analysis=null;setTimeout(()=>location.hash="#/dashboard",600)}catch(e){status.textContent=e.message;notify(e.message,"error")}}
function filter(){const t=document.querySelector("#typeFilter")?.value||"",s=document.querySelector("#severityFilter")?.value||"",q=(document.querySelector("#search")?.value||"").toLowerCase();const list=state.detections.filter(f=>(!t||f.event.event_type===t)&&(!s||f.severity===s)&&(!q||JSON.stringify(f).toLowerCase().includes(q)));document.querySelector("#queue").innerHTML=queue(list);document.querySelectorAll("[data-event]").forEach(b=>b.onclick=()=>showDetail(b.dataset.event))}
async function render(reload=true){document.querySelectorAll("#topnav a").forEach(a=>a.classList.toggle("active",a.dataset.route===route()));app.innerHTML='<div class="loading">Loading dynamic evidence platform…</div>';try{if(reload)await load();const r=route(),views={home:landing,upload,dashboard,detections,evidence,scada,ai,devsecops,integrity:()=>detections("integrity"),safeguards,reports,monitoring,audit};app.innerHTML=r==="compliance"?await compliance():await (views[r]||landing)();bind()}catch(e){app.innerHTML=`<div class="fatal"><h2>Platform unavailable</h2><p>${esc(e.message)}</p><button class="button" onclick="location.reload()">Retry</button></div>`}}
document.addEventListener("input",e=>{if(e.target.id==="auditSearch"){const q=e.target.value.toLowerCase();api("/api/audit?limit=500&search="+encodeURIComponent(q)).then(rows=>{document.querySelector("#auditTable").innerHTML=auditTable(rows);document.querySelector("#auditCount").textContent=rows.length+" entries"})}});
window.addEventListener("hashchange",()=>render());document.querySelector("#menu").onclick=()=>document.querySelector("#topnav").classList.toggle("open");render();
